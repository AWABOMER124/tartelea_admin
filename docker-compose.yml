services:
  tartelea-admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tartelea-admin
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}

